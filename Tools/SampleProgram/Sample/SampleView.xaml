<Grid x:Class="net.derammo.Helios.SampleProgram.Sample.SampleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:net.derammo.Helios.SampleProgram.Sample"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=local:SampleViewModel, IsDesignTimeCreatable=True}"
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel>
        <TextBox Text="{Binding Path=Data.SomeData}"></TextBox>
        <TextBox Text="{Binding Path=Data.SomeInteger}"></TextBox>
        <TextBox Text="{Binding SomeViewState}"></TextBox>
        <TextBox Text="{Binding SomeOtherViewState}"></TextBox>
        <Button Content="Undo" Command="{Binding UndoCommand}" Width="120"></Button>
    </StackPanel>
</Grid>
