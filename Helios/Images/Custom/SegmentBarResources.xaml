<?xml version="1.0" encoding="UTF-8"?>
<ResourceDictionary
xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
        <Style x:Key="LED1" TargetType="{x:Type Canvas}">
            <Setter Property="Visibility" Value="Visible" />
            <Setter Property="Background" Value="#FF06E409" />
            <Setter Property="Margin" Value="0,1,0,1" />
            <Setter Property="Height" Value="16" />
            <Setter Property="Width" Value="40" />
        </Style>
        <Style x:Key="LED2" TargetType="{x:Type Canvas}">
            <Setter Property="Visibility" Value="Visible" />
            <Setter Property="Background" Value="#FFE4E409" />
            <Setter Property="Margin" Value="0,1,0,1" />
            <Setter Property="Height" Value="16" />
            <Setter Property="Width" Value="40" />
        </Style>
        <Style x:Key="LED3" TargetType="{x:Type Canvas}">
            <Setter Property="Visibility" Value="Visible" />
            <Setter Property="Background" Value="#FFE40809" />
            <Setter Property="Margin" Value="0,1,0,1" />
            <Setter Property="Height" Value="16" />
            <Setter Property="Width" Value="40" />
        </Style>
</ResourceDictionary>